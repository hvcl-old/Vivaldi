def median(volume, x, y, z):
	cubic_iter = make_cube_iter(x,y,z, 1)
	meidan = get_median(volume, cubic_iter)
	return median

def main():                                                                         
	volume = load_data_3d(DATA_PATH+'/em.dat',float)

	print volume

	result = median(volume,x,y,z).range(volume).dtype(volume,float)
	
	print result
